{
  "mcpServers": {
    "orange-remote": {
      "type": "http",
      "url": "https://orange-api-x38s.onrender.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${ORANGE_MCP_TOKEN}"
      }
    },
    "orange-local": {
      "type": "stdio",
      "command": "${ORANGE_PYTHON:-/absolute/path/to/orange/venv311/bin/python}",
      "args": ["-m", "core.mcp_server.server"],
      "env": {
        "PYTHONPATH": "${ORANGE_ROOT:-/absolute/path/to/orange}",
        "ORANGE_USER_EMAIL": "${ORANGE_USER_EMAIL}"
      }
    }
  }
}
